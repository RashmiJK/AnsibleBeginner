---
  - name: "My first play"
    hosts: localhost

    tasks:

        - name: "reachability test"
          ping:

        - name: "install stress"
          homebrew:
            name: stress
            state: present